# Docker Sources

## Domains

Source:

```text
https://docs.docker.com/desktop/setup/allow-list/
```

Reason:

```text
Docker documents domains to allow for Docker Desktop, Docker Hub, authentication, updates, and pull/push operations.
```

## CIDR

```text
none
```

Reason:

```text
Docker pull and update traffic uses shared CDN and cloud infrastructure, so broad provider ranges are not added here.
```

