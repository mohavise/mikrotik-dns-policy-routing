# Messaging Group

Messaging services belong here.

Current services:

```text
signal
telegram
whatsapp
```

Services are added only after their own folder exists under `services/`.

Profile address-list pattern:

```text
DST-MESSAGING-TO-OUTBOUND
```
