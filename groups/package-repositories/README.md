# Package Repositories Group

Linux package repositories and container package/registry services belong here.

Current services:

```text
ubuntu
debian
redhat
proxmox
docker
```

Services are added only after their own folder exists under `services/`.

Profile address-list pattern:

```text
DST-PACKAGE-REPOSITORIES-TO-OUTBOUND
```

