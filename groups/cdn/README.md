# CDN Group

CDN and edge-network services belong here.

Planned examples:

```text
cloudflare
```

Services are added only after their own folder exists under `services/`.

Profile address-list pattern:

```text
DST-CDN-TO-OUTBOUND
```

