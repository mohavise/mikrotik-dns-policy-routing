# Mobile App Store Group

Mobile app store services belong here.

Current services:

```text
apple-app-store
```

Services are added only after their own folder exists under `services/`.

Profile address-list pattern:

```text
DST-MOBILE-APP-STORE-TO-OUTBOUND
```
